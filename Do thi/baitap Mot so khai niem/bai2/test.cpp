#include <iostream>
#include <vector>
#include <bits/stdc++.h>
using namespace std;

int main(){
    ifstream f;
    string s;
    f.open("input1.txt", ios::in);
    fflush(stdin);
    getline(f,s);
    getline(f,s);
    for(int i = 0; i < )
    cout << s;
}